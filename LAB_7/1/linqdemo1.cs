using System;
using System.Collections.Generic;
using System.Linq;

namespace LINQdemo1_2
{
    class linqdemo1
    {
        /*static void Main(string[] args)
        {
            List<int> numlist = new List<int>();
            for(int i=1; i<=100; i++)
            {
                numlist.Add(i);
            }
            var evennum = numlist.Where(n => n % 2 == 0);       //for even numbers
            var oddnum = numlist.Where(n => n % 2 == 1);        //for odd numbers
            var num = numlist.Select(n => n);                   //for all numbers
            var max = numlist.Max();                            //for max number
            var min = numlist.Min();                            //for min number
            var avg = numlist.Average();                       //for avgerage value
            Console.WriteLine("The even no. are:");
            foreach (int i in evennum)
                Console.Write(i+"\t");
            Console.WriteLine();

            Console.WriteLine("The odd no. are:");
            foreach (int i in oddnum)
                Console.Write(i + "\t");
            Console.WriteLine();

            Console.WriteLine("The no. are:");
            foreach (int i in num)
                Console.Write(i + "\t");
            Console.WriteLine();

            Console.WriteLine("The max no. is:");
            Console.WriteLine(max);

            Console.WriteLine("The min no. is:");
            Console.WriteLine(min);

            Console.WriteLine("The average is:");
            Console.WriteLine(avg);

            Console.ReadKey();
        }*/
    }
}
